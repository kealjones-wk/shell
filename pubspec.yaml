name: shell
dependencies:
  js: any
  over_react: ^3.0.0

dev_dependencies:
  build_runner: ">=1.6.2 <2.0.0"
  build_test:
  build_web_compilers:

dependency_overrides:
  shell_events:
    path: ../shell_events
